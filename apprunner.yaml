version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci --only=production
run:
  runtime-version: 20
  command: node src/index.js
  network:
    port: 3000
    env: APP_PORT
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3000"

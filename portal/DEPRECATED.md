# ⚠️ DEPRECATED — iFu Labs Portal

This app has been superseded by **Ghara** (`ghara/`), the unified product.

- Login/signup → `ghara/src/app/login/` and `ghara/src/app/signup/`
- Onboarding → `ghara/src/app/onboarding/`
- Billing → `ghara/src/app/(app)/billing/`
- Invite acceptance → `ghara/src/app/invite/[token]/`
- Password reset → `ghara/src/app/reset-password/[token]/`

**Do not add new features here.** All new work goes into `ghara/`.

This directory will be deleted 60 days after Ghara is confirmed stable in production.
